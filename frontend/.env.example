# URL del backend (API). Solo necesario en producción o si no usas el proxy de Vite.
# En desarrollo con "npm run dev" el proxy envía /api a localhost:3000.
# En producción (Netlify, etc.) pon aquí la URL del backend, ej. https://tu-api.onrender.com
VITE_API_URL=
